# 2026-05-19 初期調査メモ

## 結論

`PDF分割命名ツール_v3_1_1` の元ソースは、確認した主要ローカル開発置き場では見つかりませんでした。

ただし、共有フォルダ上のビルド済み配布物は PyInstaller one-folder 形式で、入口モジュール `pdf_splitter_app.py` のバイトコードを抽出できました。元ソースがなくても、抽出済みバイトコードを足がかりに仕様復元と再構築は可能です。

## 確認した配布物

- 配布フォルダ: `\\ao100\103_共有\アプリケーション\自社開発シリーズ\PDF分割命名ツール_v3_1_1`
- 実行ファイル: `PDF分割命名ツール_v3_1_1.exe`
- SHA256: `929C82BE484A1AA1F3D16E1DE03E9286E4C41A6998907641C2BCAF8C6FF1F08F`
- 配布構成: EXE + `_internal`

## 技術構成

- Python 3.14 系
- PyInstaller
- Tkinter GUI
- PyMuPDF / fitz
- PIL
- openpyxl
- pandas / numpy

## EXE内で確認した主な情報

- 入口モジュール: `pdf_splitter_app.py`
- アプリ表示名: `Universal PDF Meta-Splitter (Wizard Mode)`
- 状態ファイル: `_pdf_split_state.json`
- プリセットファイル: `presets.json`
- 既定テンプレート: `{box_no:0>2}_{binder_no:0>2}_{seq:0>3}_{company}_{doc}.pdf`

## 確認できた主な機能

- PDFの個別選択または入力フォルダ選択
- 出力フォルダ指定
- PDFページプレビュー
- OCRテキスト表示
- 会社名などのメタデータ入力
- 手動分割
- 定型分割
- 白紙分割
- 分割単位ごとの命名
- 出力前チェック
- 一括出力
- 状態保存と復元

## 抽出済み解析ファイル

- `artifacts/analysis/pdf_splitter_app.pyc`

## 改修方針の評価

### 小規模改修

復元ベースで対応可能です。画面文言、入力チェック、命名ルール、出力前チェックなどの局所変更は現実的です。

### 中規模以上の改修

バイトコードから直接改修を続けるより、現在の挙動を仕様化し、`recovery/` 配下に新しいPythonプロジェクトとして再構築する方が安全です。

### 推奨

まずは `pdf_splitter_app.pyc` から画面・関数・データ構造を復元し、`docs/` に現行仕様を整理します。その後、改修内容が小さい場合は復元ソースを整備して対応し、大きい場合は再構築に切り替えます。
