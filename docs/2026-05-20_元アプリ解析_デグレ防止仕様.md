# 元アプリ解析・デグレ防止仕様

作成日: 2026-05-20
対象Issue: #29
対象元アプリ: `PDF分割命名ツール_v3_1_1.exe`

## 1. 目的

UI/UX刷新や吉田エルシス向け改修を進める前に、元アプリが持っていた業務上必要な機能、導線、安全条件を整理し、復元版でデグレを起こさないための基準を定義する。

本書では、元アプリと復元版の差分を以下に分類する。

- `復元済み`: 現在の復元版で実装済み。
- `仕様変更`: 現場FBにより意図的に変更したもの。
- `必須復元`: 元アプリ主要価値または安全性に関わるため復元が必要。
- `後回し可`: 便利機能だが初期運用を止めるほどではない。
- `要再確認`: 元アプリ内に痕跡はあるが、実機操作で詳細確認が必要。

## 2. 証拠

| 証拠 | 内容 |
| --- | --- |
| 実機起動 | 共有フォルダ上の元EXEを起動し、Step 1画面を確認。 |
| スクリーンショット | `artifacts/ui-screenshots/original-app-window.png` |
| 抽出済みコード情報 | `artifacts/analysis/pdf_splitter_app.pyc` の文字列・関数名を確認。 |
| 既存仕様書 | `docs/2026-05-19_アプリ仕様書.md` |
| 現在復元版 | `recovery/pdf_splitter_tool/` |

元EXE:

```text
\\ao100\103_共有\アプリケーション\自社開発シリーズ\PDF分割命名ツール_v3_1_1\PDF分割命名ツール_v3_1_1.exe
```

元アプリ画面タイトル:

```text
Universal PDF Meta-Splitter (Wizard Mode)
```

## 3. 元アプリの業務フロー

元アプリはウィザード形式で、以下の流れを持つ。

1. Step 1: 処理対象PDF、案件プリセット、共通項目、出力先を選択する。
2. Step 2: PDFをプレビューし、手動分割、定型分割、白紙分割、OCR検索/インデックス候補検索で分割範囲を作る。
3. Step 3: 分割単位ごとにメタデータを入力し、生成予定ファイル名を確認する。
4. Step 4: 出力前チェックを行い、一括出力し、ログ・進捗・出力結果を確認する。
5. Ctrl+Sで状態保存し、次回起動時に状態復元できる。

画面下部には以下の操作ヒントがある。

```text
Ctrl+S 保存  /  Ctrl+Z,Y Undo/Redo  /  Alt+←,→ PDF移動
```

## 4. Step別デグレ防止要件

### 4.1 Step 1: 処理対象と出力先

元アプリで確認された主な導線:

- 入力フォルダ選択
- PDF個別選択
- ファイル名で自動ソート
- 読込進捗
- 読込中断
- 選択解除
- 置換読込(フォルダ)
- 置換読込(PDF)
- 全クリア
- 出力フォルダ指定
- 案件プリセット選択
- 共通項目入力
- 共通項目を全PDFへ適用
- 最終保存状態表示

復元版との差分:

| 項目 | 状態 | 判定 | 対応 |
| --- | --- | --- | --- |
| PDF個別選択 | 実装済み | 復元済み | 維持 |
| 入力フォルダ選択 | 実装済み | 復元済み | 維持 |
| 出力フォルダ指定 | 実装済み | 復元済み | 維持 |
| プリセット選択 | 実装済み | 復元済み | 維持 |
| 共通項目を全PDFへ適用 | Step 1下書き入力と全PDF適用を実装済み | 復元済み | #32 |
| 選択解除/置換読込/全クリア | 実装済み | 復元済み | #32 |
| ファイル名自動ソート | 実装済み | 復元済み | #32 |
| 読込進捗/中断 | 実装済み | 復元済み | #32 |
| 最終保存状態表示 | 状態保存表示を実装済み | 復元済み | #36 |

### 4.2 Step 2: 分割設定・PDFプレビュー

元アプリで確認された主な導線:

- PDFプレビュー
- OCRテキスト表示
- ページ一覧
- ページ送り
- 現在ページ入力または高速移動
- ズーム
- 現在ページの前で区切る
- 定型分割
- 白紙分割
- OCR通常検索
- インデックス候補検索
- 検索語/キーワードのプレビュー上ハイライト
- OCR選択テキスト転記
- Undo/Redo
- 分割削除
- 参照元フォルダを開く
- 現在PDFのファイル名変更

復元版との差分:

| 項目 | 状態 | 判定 | 対応 |
| --- | --- | --- | --- |
| PDFプレビュー | 実装済み | 復元済み | 維持 |
| OCRテキスト表示 | 実装済み | 復元済み | 維持 |
| ページ送り | 実装済み | 復元済み | 維持 |
| Spaceで現在ページ前に分割 | 実装済み | 復元済み | 維持 |
| 定型分割 | 実装済み | 復元済み | 維持 |
| 白紙候補検出 | 実装済み | 条件付き復元済み | 現行しきい値 `0.985` と利用者確認前提で条件付きOK。現場PDFで問題が出た場合は再調整 |
| OCR検索/インデックス候補 | 実装済み | 復元済み | 事前OCR済みPDFまたはテキスト層ありPDFが入力条件 |
| ページ状態の視認性 | PR #28で改善済み | 復元済み | PR #28 |
| 検索語ハイライト | PDFプレビュー上の矩形ハイライトを実装済み | 復元済み | #30。テキスト層ありPDFが入力条件 |
| OCR選択テキスト転記 | OCR本文の選択文字列を任意メタデータ項目へ転記可能 | 復元済み | #31。テキスト層ありPDFが入力条件 |
| ズーム/表示モード | ズーム、幅に合わせる、全体表示を実装済み | 復元済み | #33 |
| ページ番号入力/高速移動 | ページ指定移動を実装済み | 復元済み | #33 |
| Undo/Redo | Ctrl+Z/Yとボタン操作を実装済み | 復元済み | #33 |
| 分割削除 | 選択分割削除と最後の分割取り消しを実装済み | 復元済み | #33 |
| 参照元フォルダを開く | 実装済み | 復元済み | #33 |
| 元PDFファイル名変更 | 確認ダイアログ付きでファイル名変更と参照再マップを実装済み | 復元済み。ただし実PDF受入確認が必要 | #36 |

### 4.3 Step 3: メタデータ入力

元アプリで確認された主な導線:

- 分割単位一覧
- 箱No、バインダーNo、連番、会社名、書類名の入力
- 会社名/書類名の候補表示
- 前行メタデータコピー
- 未解決行ナビゲーション
- OCR選択テキスト転記
- 生成予定ファイル名表示
- 生成予定フォルダを開く

復元版との差分:

| 項目 | 状態 | 判定 | 対応 |
| --- | --- | --- | --- |
| セグメント単位入力 | 実装済み | 復元済み | 維持 |
| 箱No/バインダーNo/連番 | 実装済み | 復元済み | 維持 |
| 会社名/書類名 | 任意項目として保持 | 仕様変更 | 吉田エルシス既定名には使わない |
| 一括入力/再採番 | 実装済み | 復元済み | 維持 |
| セグメント表 | PR #28で改善済み | 復元済み | PR #28 |
| 前行コピー | 実装済み | 復元済み | #34 |
| 未解決行ナビゲーション | 次の要修正行へ移動する導線を実装済み | 復元済み | #34 |
| 入力補助候補 | OCR本文から候補表示する簡易導線を実装済み | 部分復元。現場運用で有効性確認が必要 | #34 |
| OCR選択テキスト転記 | 実装済み | 復元済み | #31 |
| 生成予定フォルダを開く | 実装済み | 復元済み | #34 |

### 4.4 Step 4: 出力前確認・一括出力

元アプリで確認された主な導線:

- 出力前チェック
- 修正が必要な項目の表示
- 一括出力
- 出力中進捗
- 現在処理中ファイル
- 出力ログ
- 既存出力の再利用/スキップ
- 出力フォルダを開く
- SHA256表示

復元版との差分:

| 項目 | 状態 | 判定 | 対応 |
| --- | --- | --- | --- |
| 出力前チェック | 実装済み | 復元済み | 維持 |
| 未入力/命名エラー停止 | 実装済み | 復元済み | 維持 |
| 一括出力 | 実装済み | 復元済み | 維持 |
| 重複回避 | 実装済み | 復元済み | 維持 |
| チェックリスト表示 | PR #28で改善済み | 復元済み | PR #28 |
| 出力進捗 | 実装済み | 復元済み | #35 |
| 出力ログの詳細化 | 成功/失敗ログとSHA256表示を実装済み | 復元済み | #35 |
| 出力フォルダを開く | 実装済み | 復元済み | #35 |
| SHA256 | 出力PDFごとに表示 | 復元済み | #35 |
| 既存出力再利用/スキップ | 重複回避は実装済み。再利用/スキップ運用は動作要件を要確認 | 要再確認 | 後続Issue |

## 5. 横断要件

### 5.0 OCR入力条件

復元版では、画像PDFに対するOCRエンジン実行は非スコープとする。
OCR検索、検索語ハイライト、OCR本文表示、OCR選択テキスト転記は、事前OCR済みPDFまたは既存テキスト層があるPDFを入力条件とする。
テキスト層がないPDFでは、利用者へ事前OCR済みPDFの利用を案内し、分割・命名・出力の通常作業は継続できるようにする。

### 5.1 状態保存・復元

元アプリで確認された要素:

- `_pdf_split_state.json`
- `_pdf_split_state.bak.json`
- `_pdf_split_state.tmp`
- `作業状態を保存しました。`
- `前回状態を復元しました`
- `バックアップから復元`
- 旧形式状態ファイルの非対応表示

復元版は `StateManager` をGUIフローへ統合済み。Ctrl+Sまたは保存操作で状態保存し、起動時に前回状態を復元する。状態ファイルが壊れている場合はバックアップ復元を試みる。

対応: #36

### 5.2 元PDFリネーム

元アプリには `rename_current_pdf_file`、`_remap_path_references_after_rename` の痕跡がある。復元版は同等の安全方針で実装済み。
元PDF本文は変更しない前提を維持し、ファイル名変更だけを条件付きで許可する。

必須条件:

- 実行前に確認ダイアログを出す。
- 成功後、PDF一覧、分割範囲、OCR/検索キャッシュ、状態ファイル内参照を再マップする。
- 失敗時は旧パス参照を維持し、利用者に理由を表示する。

対応: #36

### 5.3 ショートカット

元アプリの画面下部表示:

- Ctrl+S: 保存
- Ctrl+Z/Y: Undo/Redo
- Alt+←/→: PDF移動

復元版ではStep 2のSpace/左右キーは実装済みだが、元アプリのショートカット互換は未達。
ショートカットは入力欄、検索欄、OCR本文操作を妨げないことを必須条件とする。

PR #38でCtrl+S、Ctrl+Z/Y、Deleteによる主要ショートカットを実装済み。Alt+←/→によるPDF移動は元アプリ互換の追加候補として、実PDF受入確認後に必要性を判断する。

対応: #33, #36

## 6. PR #28 デグレ確認結果

PR #28 は、復元版の現行主要フローを見やすくするUI/UX改善であり、以下の点ではデグレを起こしていない。

- PDF選択から分割、メタデータ入力、出力前チェック、一括出力までの現行復元フローを維持している。
- Step 2のページ状態、候補、分割位置の視認性を改善している。
- Step 3でセグメント状態を一覧確認できるようにしている。
- Step 4で出力可否と要修正理由を確認しやすくしている。
- プリセット管理の組み込み保護を分かりやすくしている。

PR #28単体では元アプリ完全互換を達成していなかったが、PR #38で以下の未復元差分を実装した。

- OCR検索語ハイライト: #30
- OCR選択テキスト転記: #31
- Step 1取込/共通項目/読込管理: #32
- Step 2ズーム/Undo/Redo/ページ操作: #33
- Step 3入力補助/前行コピー/未解決行ナビゲーション: #34
- Step 4出力進捗/ログ/SHA256: #35
- 状態保存/復元/元PDFリネーム再マップ: #36

## 7. 受入確認・配布前の残課題

PR #38時点で #30〜#36 は実装済み。ただし、配布判断前に以下を別Issueで確認する。

- 実PDF受入確認: `006_18.pdf` と `000高精度ocrのテスト用ファイル.pdf` で主要フロー、状態保存/復元、大容量PDF操作継続性を確認する。OCRハイライトとOCR転記は、事前OCR済みPDFまたはテキスト層ありPDFで別途確認する。
- 配布準備: PyInstaller one-folderを新バージョン名で作成し、既存共有フォルダ配布物を上書きしない。
- GUI品質保証: 主要画面のスモークとスクリーンショット確認基準を強化する。
- 白紙検出精度: 現状の白紙候補検出と白紙スキップ相当の運用は条件付きOKとする。条件は、候補を利用者が確認してから分割/スキップ判断すること、現場PDFで薄い印字・罫線・ノイズの誤判定が出た場合は別Issueで再調整すること。
- 保守性改善: `app.py` 肥大化をStep別責務へ分割する。ただし配布前ブロッカーがなければ後回しにする。

## 8. マージ判断

PR #28 は、以下を満たす場合に「UI/UX改善PR」としてマージ可能と判断する。

1. PR #28が元アプリ完全互換ではないことをPR本文で明記する。
2. 本書の差分表と #30〜#36 が、未復元差分の追跡先としてリンクされている。
3. PR #28の検証結果に、元アプリ主要価値を壊していないことを記録する。
4. UI刷新後も #30〜#36 の復元実装が可能な画面構造であることを確認する。

逆に、PR #28を「元アプリ互換完了」とみなしてマージすることは禁止する。

## 9. 対応済みIssue

| Issue | 内容 |
| --- | --- |
| #30 | OCR検索語をPDFプレビュー上でハイライト表示する |
| #31 | 選択OCRテキストをメタデータへ転記できるようにする |
| #32 | Step 1のPDF取込・共通項目適用・読込管理を元アプリ水準へ戻す |
| #33 | Step 2のページ操作・ズーム・Undo/Redoを元アプリ水準へ戻す |
| #34 | Step 3の入力補助・前行コピー・未解決行ナビゲーションを復元する |
| #35 | Step 4の出力進捗・ログ・出力フォルダ操作・SHA256を復元する |
| #36 | 状態保存・復元・元PDFリネーム時の参照再マップを元アプリ水準へ戻す |
