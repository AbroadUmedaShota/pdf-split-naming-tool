# 受入結果 2026-06-18

## 判定

受入は Pass。

追加指定の本番PDFや別業務ルールが出た場合のみ、同じコマンドで追試する。

## 配布

- PR #103 は `main` へマージ済み。
- GitHub Release `v0.1.4` は公開済み。
- 公開assetは `latest.json`、`pdf-organizer-desktop_0.1.4_x64-setup.exe`、`pdf-organizer-desktop_0.1.4_x64-setup.exe.sig` の3件。
- `latest.json` は `releases/latest/download/latest.json` でHTTP 200を確認済み。
- installer はHTTP 200、Content-Length `97856384` を確認済み。

## 実行済み確認

| 領域 | コマンド/確認 | 結果 |
|---|---|---|
| updater | `npm run test:installed-updater` | Pass。v0.1.3からv0.1.4を検出、インストール、再起動後 `現在のバージョン: 0.1.4`、再確認時 `最新版です。` |
| インストール版コア | `npm run test:installed-core` | Pass。生成2ページPDFの取込、分割、命名、出力前チェック、2件出力、作成済み表示、二重出力ガード |
| 複数PDF | `PDF_ORGANIZER_INSTALLED_CORE_MULTI_PDF=1 npm run test:installed-core` | Pass。2つのPDFを同時取込し、3セグメント、3件出力 |
| 260ページPDF | `PDF_ORGANIZER_INSTALLED_CORE_PDF_PATH=..\..\data\issue-9\high_load_260p_scan_like.pdf`、`PDF_ORGANIZER_INSTALLED_CORE_EXPECTED_PAGES=260` 付き `npm run test:installed-core` | Pass。260ページ表示、分割、2件出力 |
| 出力復旧 | `npm run test:installed-output-recovery` | Pass。出力フォルダをWindows Explorerで開く、初回2件成功/1件失敗、失敗理由表示、失敗分のみ再出力、3件作成済み |
| STEP1重点 | `npm run test:installed-step1` | Pass。パスワードPDF、PDF以外/存在しないパス、複数PDF一部失敗、重複排除、除外、全クリア、再取込、STEP2遷移 |
| 状態保存 | `npm run test:installed-state` | Pass。保存、再起動、復元、分割と入力値復元 |
| 実ダイアログ | `npm run test:installed-native-dialog` | Pass。Windowsの実PDF選択ダイアログと実フォルダ選択ダイアログを操作 |
| 現場相当PDF | `scripts\sample-pdf-smoke.ps1 -PdfPath <発注書PDF>` | Pass。`page_count=1`、preview、preflight、1件export、state roundtrip |
| ブラウザE2E | `npm run test:e2e` | Pass。24件 |
| desktop回帰 | `npm run test:desktop` | Pass |
| 型検査 | `npm run typecheck` | Pass |
| リリースasset検査 | `npm run release:check` | Pass |
| 集約検証 | `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify.ps1` | Pass |

## 受入表への反映

- A1-A8 は技術受入 Pass。
- B1-B8、C1-C4 は自動/実機スモーク/ロジック確認 Pass。
- B1 は `test:installed-output-recovery` で一時出力先がWindows Explorerに開くことを確認済み。
- B4 は `test:installed-output-recovery` で初回2件成功/1件失敗、失敗分のみ再出力、3件作成済みまで確認済み。
- 追加指定PDFがある場合は、`sample-pdf-smoke.ps1` または `PDF_ORGANIZER_INSTALLED_CORE_PDF_PATH` 付き `test:installed-core` で追試する。
